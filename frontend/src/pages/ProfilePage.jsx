import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { useAuth } from '../AuthContext.jsx';import AppShell from '../components/AppShell.jsx';
export default function ProfilePage(){const{user,logout}=useAuth();const navigate=useNavigate();const[loggingOut,setLoggingOut]=useState(false);const[error,setError]=useState('');async function signOut(){if(loggingOut)return;setLoggingOut(true);setError('');try{await logout();navigate('/login',{replace:true});}catch{setError('Unable to sign out right now. Please try again.');setLoggingOut(false);}}const initial=(user.fullName||user.username||'U').charAt(0).toUpperCase();return <AppShell eyebrow="Your account" title="Profile"><section className="profile-card"><header><span className="profile-avatar" aria-hidden="true">{initial}</span><div><h2>{user.fullName||user.username}</h2><p>@{user.username}</p><span className="badge active">Exhibitor</span></div></header><dl className="account-details"><div><dt>Full name</dt><dd>{user.fullName||'Not provided'}</dd></div><div><dt>Username</dt><dd>{user.username}</dd></div><div><dt>Email</dt><dd>{user.email?<a href={`mailto:${user.email}`}>{user.email}</a>:'Not provided'}</dd></div><div><dt>Role</dt><dd>Exhibitor</dd></div></dl></section><section className="account-card" aria-labelledby="account-heading"><div><h2 id="account-heading">Account</h2><p>Your profile is managed by the event organizer.</p></div>{error&&<div className="error-message" role="alert">{error}</div>}<button className="secondary-button danger-button" type="button" onClick={signOut} disabled={loggingOut}>{loggingOut?'Signing out…':'Log out'}</button></section></AppShell>}
