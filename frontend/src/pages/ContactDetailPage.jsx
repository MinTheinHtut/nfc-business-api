import { useEffect,useState } from 'react';import { Link,useParams } from 'react-router-dom';import { getContact } from '../api.js';import AppShell,{CompanyAvatar,ErrorState,LoadingState} from '../components/AppShell.jsx';
export default function ContactDetailPage(){const{id}=useParams();const[contact,setContact]=useState(null);const[error,setError]=useState('');useEffect(()=>{getContact(id).then(data=>setContact(data.contact)).catch(()=>setError('Unable to load this contact. It may no longer be available.'));},[id]);return <AppShell eyebrow="Contact details" title="Business profile"><Link className="back-link" to="/contacts">← Back to contacts</Link>{error?<ErrorState>{error}</ErrorState>:!contact?<LoadingState>Loading contact…</LoadingState>:<article className="detail-card"><header><CompanyAvatar company={contact} large/><div><h2>{contact.company_name}</h2><p>{[contact.industry,contact.country].filter(Boolean).join(' · ')||'Business contact'}</p><span className="badge active">Saved {new Date(contact.confirmed_at).toLocaleString()}</span></div></header>{contact.description&&<section><h3>About</h3><p>{contact.description}</p></section>}<dl className="detail-grid">{contact.contact_name&&<div><dt>Contact person</dt><dd>{contact.contact_name}{contact.contact_position&&<small>{contact.contact_position}</small>}</dd></div>}{contact.email&&<div><dt>Email</dt><dd><a href={`mailto:${contact.email}`}>{contact.email}</a></dd></div>}{contact.phone&&<div><dt>Phone</dt><dd><a href={`tel:${contact.phone}`}>{contact.phone}</a></dd></div>}{contact.website&&<div><dt>Website</dt><dd><a href={contact.website} target="_blank" rel="noopener noreferrer">Open website <span aria-hidden="true">↗</span></a></dd></div>}{contact.address&&<div><dt>Address</dt><dd>{contact.address}</dd></div>}</dl></article>}</AppShell>}
