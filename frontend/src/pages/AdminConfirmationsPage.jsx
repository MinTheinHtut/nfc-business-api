import { useEffect,useState } from 'react'; import { apiRequest } from '../api.js'; import AdminHeader from '../components/AdminHeader.jsx';
export default function AdminConfirmationsPage(){const [items,setItems]=useState([]);const [error,setError]=useState('');useEffect(()=>{apiRequest('/admin/confirmations').then(x=>setItems(x.confirmations)).catch(e=>setError(e.message));},[]);return <div className="admin-page"><AdminHeader/><main className="admin-content"><div className="page-heading"><div><span className="eyebrow">Organizer</span><h1>Confirmations</h1></div></div>{error&&<div className="error-message">{error}</div>}<div className="table-panel"><table><thead><tr><th>Record</th><th>Exhibitor</th><th>Visitor / Company</th><th>Confirmed At</th></tr></thead><tbody>{items.map(x=><tr key={x.record_id}><td>#{x.record_id}</td><td><strong>{x.username}</strong><br/><small>{x.full_name}</small></td><td>{x.company_name}</td><td>{new Date(x.confirmed_at).toLocaleString()}</td></tr>)}</tbody></table>{!items.length&&!error&&<p className="empty-state">No confirmations yet.</p>}</div></main></div>}
