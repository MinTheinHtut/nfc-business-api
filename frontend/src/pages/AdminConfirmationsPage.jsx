import { useEffect,useState } from 'react';import { getAdminConfirmations } from '../api.js';import AdminHeader from '../components/AdminHeader.jsx';import { EmptyState,ErrorState,LoadingState } from '../components/AppShell.jsx';
export default function AdminConfirmationsPage(){const[items,setItems]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{getAdminConfirmations().then(data=>setItems(data.confirmations)).catch(e=>setError(e.message)).finally(()=>setLoading(false));},[]);return <div className="admin-page"><AdminHeader/><main className="admin-content"><div className="page-heading"><div><span className="eyebrow">Organizer</span><h1>Confirmations</h1><p>Connections saved by exhibitors during the event.</p></div></div>{loading?<LoadingState>Loading confirmations…</LoadingState>:error?<ErrorState>{error}</ErrorState>:!items.length?<EmptyState title="No confirmations yet">Saved business connections will appear here.</EmptyState>:<div className="table-panel responsive-table"><table><thead><tr><th>Status</th><th>Exhibitor</th><th>Company</th><th>Date and time</th></tr></thead><tbody>{items.map(item=><tr key={item.record_id}><td data-label="Status"><span className="badge active">Confirmed</span></td><td data-label="Exhibitor"><strong>{item.full_name||item.username}</strong>{item.full_name&&<small>{item.username}</small>}</td><td data-label="Company">{item.company_name}</td><td data-label="Date and time"><time dateTime={item.confirmed_at}>{new Date(item.confirmed_at).toLocaleString()}</time></td></tr>)}</tbody></table></div>}</main></div>}
