import { useState } from 'react';import { Link,NavLink,useNavigate } from 'react-router-dom';import { useAuth } from '../AuthContext.jsx';import { LanguageSwitcher,useI18n } from '../i18n/I18nContext.jsx';import CompanyLogo from './CompanyLogo.jsx';
export default function AppShell({title,eyebrow,children}){const{user,logout}=useAuth();const{t}=useI18n();const navigate=useNavigate();const[loggingOut,setLoggingOut]=useState(false);const[logoutError,setLogoutError]=useState('');const navigation=[['/dashboard','nav.home','⌂'],['/contacts','nav.contacts','◎'],['/profile','nav.profile','○']];async function signOut(){if(loggingOut)return;setLoggingOut(true);setLogoutError('');try{await logout();navigate('/login',{replace:true});}catch{setLogoutError(t('session.logoutError'));setLoggingOut(false);}}const links=navigation.map(([to,key,icon])=><NavLink key={to} to={to} end={to==='/dashboard'}><span aria-hidden="true">{icon}</span><span>{t(key)}</span></NavLink>);return <div className="app-layout"><aside className="app-sidebar"><Link className="brand-lockup" to="/dashboard"><span className="brand-mark">N</span><span>NFC Connect<small>{t('brand.matching')}</small></span></Link><nav aria-label={t('nav.exhibitor')}>{links}</nav><LanguageSwitcher/><div className="sidebar-user"><span className="avatar">{user?.fullName?.charAt(0)?.toUpperCase()||user?.username?.charAt(0)?.toUpperCase()||'U'}</span><div><strong>{user?.fullName||user?.username}</strong><small>{t('common.exhibitor')}</small></div></div>{logoutError&&<small className="sidebar-error" role="alert">{logoutError}</small>}<button className="sidebar-logout" type="button" onClick={signOut} disabled={loggingOut}>{t(loggingOut?'common.loggingOut':'common.logout')}</button></aside><main className="app-main"><header className="page-header"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1></div><div className="page-header-tools"><span className="event-chip">{t('brand.connected')}</span><LanguageSwitcher className="mobile-language"/></div></header>{children}</main><nav className="bottom-nav" aria-label={t('nav.mobileExhibitor')}>{links}</nav></div>}
export function CompanyAvatar({company,large=false}){return <CompanyLogo logoUrl={company.logo_url} companyName={company.company_name} size={large?'large':'medium'} className="company-avatar"/>}export function EmptyState({title,children}){return <section className="empty-state-card"><span className="empty-icon" aria-hidden="true">◇</span><h2>{title}</h2><p>{children}</p></section>}export function LoadingState({children}){const{t}=useI18n();return <div className="loading-card" role="status"><span className="spinner" aria-hidden="true"/>{children||t('common.loading')}</div>}export function ErrorState({children}){const{t}=useI18n();return <div className="error-message" role="alert">{children||t('common.error')}</div>}
