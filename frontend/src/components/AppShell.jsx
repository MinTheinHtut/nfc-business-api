import { useState } from 'react';import { Link,NavLink,useNavigate } from 'react-router-dom';import { useAuth } from '../AuthContext.jsx';
const navigation=[['/dashboard','Home','⌂'],['/contacts','Contacts','◎'],['/profile','Profile','○']];
export default function AppShell({title,eyebrow,children}){const{user,logout}=useAuth();const navigate=useNavigate();const[loggingOut,setLoggingOut]=useState(false);const[logoutError,setLogoutError]=useState('');async function signOut(){if(loggingOut)return;setLoggingOut(true);setLogoutError('');try{await logout();navigate('/login',{replace:true});}catch{setLogoutError('Unable to sign out. Please try again.');setLoggingOut(false);}}const links=navigation.map(([to,label,icon])=><NavLink key={to} to={to} end={to==='/dashboard'}><span aria-hidden="true">{icon}</span><span>{label}</span></NavLink>);return <div className="app-layout"><aside className="app-sidebar"><Link className="brand-lockup" to="/dashboard"><span className="brand-mark">N</span><span>NFC Connect<small>Business matching</small></span></Link><nav aria-label="Exhibitor navigation">{links}</nav><div className="sidebar-user"><span className="avatar">{user?.fullName?.charAt(0)?.toUpperCase()||user?.username?.charAt(0)?.toUpperCase()||'U'}</span><div><strong>{user?.fullName||user?.username}</strong><small>Exhibitor</small></div></div>{logoutError&&<small className="sidebar-error" role="alert">{logoutError}</small>}<button className="sidebar-logout" type="button" onClick={signOut} disabled={loggingOut}>{loggingOut?'Signing out…':'Log out'}</button></aside><main className="app-main"><header className="page-header"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1></div><span className="event-chip">Event connected</span></header>{children}</main><nav className="bottom-nav" aria-label="Mobile exhibitor navigation">{links}</nav></div>}
export function CompanyAvatar({company,large=false}){return company.logo_url?<img className={`company-avatar ${large?'large':''}`} src={company.logo_url} alt={`${company.company_name} logo`}/>:<span className={`company-avatar monogram ${large?'large':''}`} aria-hidden="true">{company.company_name?.charAt(0)?.toUpperCase()||'C'}</span>}
export function EmptyState({title,children}){return <section className="empty-state-card"><span className="empty-icon" aria-hidden="true">◇</span><h2>{title}</h2><p>{children}</p></section>}
export function LoadingState({children='Loading…'}){return <div className="loading-card" role="status"><span className="spinner" aria-hidden="true"/>{children}</div>}
export function ErrorState({children}){return <div className="error-message" role="alert">{children||'Something went wrong. Please try again.'}</div>}
