import { Router } from 'express';import { createVisitor,deactivateVisitor,getVisitor,listVisitors,updateVisitor } from '../controllers/admin-visitor.controller.js';import { commitVisitorImport,handleVisitorImportUpload,previewVisitorImport } from '../controllers/admin-visitor-import.controller.js';import { requireAdmin,requireAuth } from '../middleware/auth.middleware.js';
const router=Router();router.use(requireAuth,requireAdmin);router.post('/import/preview',handleVisitorImportUpload,previewVisitorImport);router.post('/import/commit',handleVisitorImportUpload,commitVisitorImport);router.get('/',listVisitors);router.get('/:id',getVisitor);router.post('/',createVisitor);router.put('/:id',updateVisitor);router.delete('/:id',deactivateVisitor);export default router;
